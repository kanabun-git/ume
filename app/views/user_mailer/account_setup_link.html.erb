<p><%= @user.shop ? "#{@user.shop.name} 様" : "#{@user.name} 様" %></p>

<p>FuzokuZeroへのご登録ありがとうございます。<br>
以下のリンクからパスワードを設定し、管理画面にログインしてください。</p>

<p>
  ログイン用メールアドレス: <%= @user.email %><br>
  <%= link_to "パスワードを設定する", @account_setup_url %><br>
  (このリンクは発行から3日間有効です)
</p>

<p>ログイン後の管理画面URL: <%= link_to (@user.shop_admin? ? shop_admin_root_url : root_url), (@user.shop_admin? ? shop_admin_root_url : root_url) %></p>
