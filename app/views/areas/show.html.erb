<% content_for :title, "#{@area.name} の店舗一覧 | FuzokuZero" %>

<h1><%= @area.name %> の店舗一覧</h1>

<div class="grid">
  <% @shops.each do |shop| %>
    <%= render "shops/shop_card", shop: shop %>
  <% end %>
</div>

<%= paginate @shops %>
