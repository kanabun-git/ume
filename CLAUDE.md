# FuzokuZero(ume)開発メモ

このリポジトリで作業するときの前提。設計の詳細は [README.md](README.md) と
`docs/` 配下(構成は [docs/architecture.md](docs/architecture.md)、本番の
構築・運用手順は [docs/vps_setup.md](docs/vps_setup.md))を参照。

## 運用者向けに書くコマンドの約束

* **テキストエディタは `vi` を使う。`nano` は使わない**(本番サーバーでの
  作業手順・ドキュメント・チャットでの説明のすべてで)。
* 本番サーバーの作業手順を書くときは、コピー&ペーストでそのまま実行できる
  形にする(プレースホルダーは `(...)` で明示する)。
* 時間のかかる作業(`bin/deploy` など)は `tmux` の中で実行する手順にする
  --- SSHが切れても処理が死なないため。

## 秘密情報

* APIキー・DBパスワード・暗号化キーの類は**リポジトリに書かない**。
  本番では `/etc/systemd/system/ume-puma.service` の `Environment=` で渡す
  (このファイル自体に秘密が入っているので扱いに注意)。
* 環境変数を追加する手順を書くときは、必ず「既存の `Environment=` 行を
  消さないこと」を添える --- 1行消えるだけでPumaが起動しなくなる。
